const db = require('../server/model/db');
const config = require('../config/hacka.config');

describe('MongoDB Connection', function () {

    it(`should connect to "${config.db.dbname}" db`, function () {

    });

});